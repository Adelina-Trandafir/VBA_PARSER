<Window x:Class="AVACONT_Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AVACONT Viewer" Width="1400" Height="800" Background="#fafafa">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT PANEL - Tree -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Search -->
            <TextBox x:Name="txtSearch" Grid.Row="0" Height="26" Margin="5"
                     FontSize="13" TextChanged="TxtSearch_TextChanged"/>

            <!-- Tree -->
            <TreeView x:Name="treeMain" Grid.Row="1" Margin="5"
                      SelectedItemChanged="TreeMain_SelectedItemChanged"
                      FontFamily="Segoe UI" FontSize="13"/>
        </Grid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>

        <!-- RIGHT PANEL -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <!-- Details -->
            <TextBox x:Name="txtDetails" Grid.Row="0" Margin="5"
                     FontFamily="Consolas" FontSize="12"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto" IsReadOnly="True"/>

            <!-- Splitter -->
            <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch"/>

            <!-- Bottom Panel -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ListBox x:Name="lstSubElements" Grid.Column="0" Margin="5"
                         SelectionChanged="LstSubElements_SelectionChanged"
                         FontFamily="Consolas" FontSize="11"/>

                <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>

                <TextBox x:Name="txtSubDetail" Grid.Column="2" Margin="5"
                         FontFamily="Consolas" FontSize="11"
                         TextWrapping="Wrap" IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </Grid>
    </Grid>
</Window>